すべてのデータがあるとき→標本分散
一部のデータから母集団を推測するとき→不偏分散
標準偏差=sqrt((データ-mean(データ))^2)/length(データ))
標準化(Z得点)= (データ-mean(データ))/標準偏差

標準偏差：sd()
相関係数：cor()
共分散：cov()
相関係数の最大値は1

標本分散：母分散の推定量としてあまり向かないが、推定量が大きくなることはあまりない
不偏分散：母分散の推定量としてはいいが、過大な推定量をとることがある
